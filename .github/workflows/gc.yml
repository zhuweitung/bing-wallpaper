name: gc

on:
  schedule:
    - cron:  '30 10 * * *'
  workflow_dispatch: # on button click
  
jobs:
  start:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    
    - name: test
      run: |
        echo $(git rev-list --objects --all | grep 25cea959e02961e9b82e0a57a717c1d01a1495b2 | awk '{print$2}')
        export filename=$(git rev-list --objects --all | grep 25cea959e02961e9b82e0a57a717c1d01a1495b2 | awk '{print$2}' | grep '^wallpaper.*jpg$')
        echo $filename
        echo $(git log --pretty=oneline --branches -- ${filename} | wc -l)
    
    - name: run gc
      run: bash rm_overdate_file.sh
      
    - name: Git auto commit
      uses: stefanzweifel/git-auto-commit-action@v4
      with:
        commit_message: Remove overdate images
        push_options: '--force'
